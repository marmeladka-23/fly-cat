using UnityEngine;

/// <summary>
/// Объект участвует в свёртке пространства, но НЕ может быть спрятан.
/// Правила относительно зоны разлома [leftX, rightX]:
///   • левее leftX            → не трогается;
///   • внутри [leftX, rightX] → переезжает на левый край шва (точно на leftX);
///   • правее rightX          → сдвигается влево на ширину свёртки (как обычный Foldable).
/// При откатывании (Unfold) объект возвращается в исходную позицию.
/// </summary>
public class Shiftable : MonoBehaviour { }
